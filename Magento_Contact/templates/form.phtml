<?php
/**
 * Copyright © Magento, Inc. All rights reserved.
 * See COPYING.txt for license details.
 */

// phpcs:disable Generic.Files.LineLength.TooLong

/** @var \Magento\Contact\Block\ContactForm $block */
if (!$block->getButtonLockManager()) {
    $objectManager = \Magento\Framework\App\ObjectManager::getInstance();
    $block->setButtonLockManager(
        $objectManager->get(\Magento\Framework\View\Element\ButtonLockManager::class)
    );
}
/** @var $escaper \Magento\Framework\Escaper */
/** @var \Magento\Contact\ViewModel\UserDataProvider $viewModel */

$viewModel = $block->getViewModel();
?>
<form class="form contact max-w-2xl"
      action="<?= $escaper->escapeUrl($block->getFormAction()) ?>"
      id="contact-form"
      method="post"
      data-hasrequired="<?= $escaper->escapeHtmlAttr(__('* Required Fields')) ?>"
      data-mage-init='{"validation":{}}'>
    <fieldset class="m-0 min-w-0 space-y-4 border-0 p-0">
        <legend class="mb-0 text-base font-semibold text-neutral-900"><span><?= $escaper->escapeHtml(__('Write Us')) ?></span></legend>
        <div class="mb-4 text-xs text-neutral-500 mt-1 no-label">
            <?= $escaper->escapeHtml(__('Jot us a note and we’ll get back to you as quickly as possible.')) ?>
        </div>
        <div class="mb-4 name required">
            <label class="block text-sm font-medium text-neutral-800 mb-1" for="name"><span><?= $escaper->escapeHtml(__('Name')) ?></span></label>
            <div class="mt-1">
                <input name="name"
                       id="name"
                       title="<?= $escaper->escapeHtmlAttr(__('Name')) ?>"
                       value="<?= $escaper->escapeHtmlAttr($viewModel->getUserName()) ?>"
                       class="w-full rounded-md border border-neutral-300 px-3 py-2 text-sm shadow-sm focus:border-blue-500 focus:ring-1 focus:ring-blue-500"
                       type="text"
                       data-validate="{required:true}"/>
            </div>
        </div>
        <div class="mb-4 email required">
            <label class="block text-sm font-medium text-neutral-800 mb-1" for="email"><span><?= $escaper->escapeHtml(__('Email')) ?></span></label>
            <div class="mt-1">
                <input name="email"
                       id="email"
                       title="<?= $escaper->escapeHtmlAttr(__('Email')) ?>"
                       value="<?= $escaper->escapeHtmlAttr($viewModel->getUserEmail()) ?>"
                       class="w-full rounded-md border border-neutral-300 px-3 py-2 text-sm shadow-sm focus:border-blue-500 focus:ring-1 focus:ring-blue-500"
                       type="email"
                       data-validate="{required:true, 'validate-email':true}"
                       data-mage-init='{"mage/trim-input":{}}'
                />
            </div>
        </div>
        <div class="mb-4 telephone">
            <label class="block text-sm font-medium text-neutral-800 mb-1" for="telephone"><span><?= $escaper->escapeHtml(__('Phone Number')) ?></span></label>
            <div class="mt-1">
                <input name="telephone"
                       id="telephone"
                       title="<?= $escaper->escapeHtmlAttr(__('Phone Number')) ?>"
                       value="<?= $escaper->escapeHtmlAttr($viewModel->getUserTelephone()) ?>"
                       class="w-full rounded-md border border-neutral-300 px-3 py-2 text-sm shadow-sm focus:border-blue-500 focus:ring-1 focus:ring-blue-500"
                       type="tel" />
            </div>
        </div>
        <div class="mb-4 comment required">
            <label class="block text-sm font-medium text-neutral-800 mb-1" for="comment">
                <span><?= $escaper->escapeHtml(__('What’s on your mind?')) ?></span>
            </label>
            <div class="mt-1">
                <textarea name="comment"
                          id="comment"
                          title="<?= $escaper->escapeHtmlAttr(__('What’s on your mind?')) ?>"
                          class="w-full rounded-md border border-neutral-300 px-3 py-2 text-sm shadow-sm focus:border-blue-500 focus:ring-1 focus:ring-blue-500"
                          cols="5"
                          rows="3"
                          data-validate="{required:true}"
                ><?= $escaper->escapeHtml($viewModel->getUserComment()) ?></textarea>
            </div>
        </div>
        <?= $block->getChildHtml('form.additional.info') ?>
    </fieldset>
    <div class="clear-both flex flex-wrap gap-3 pt-6">
        <input type="hidden" name="hideit" id="hideit" value="" />
        <button type="submit"
                title="<?= $escaper->escapeHtmlAttr(__('Submit')) ?>"
                class="submit inline-flex items-center justify-center gap-2 rounded-md border border-blue-600 bg-blue-600 px-4 py-2 text-sm font-semibold text-white transition-colors hover:bg-blue-700 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-1 focus-visible:outline-blue-600 disabled:pointer-events-none disabled:opacity-50"
                id="send2"
                <?php if ($block->getButtonLockManager()->isDisabled('contact_us_form_submit')): ?>
                    disabled="disabled"
                <?php endif; ?>>
            <span><?= $escaper->escapeHtml(__('Submit')) ?></span>
        </button>
    </div>
</form>
<script type="text/x-magento-init">
    {
        "*": {
            "Magento_Customer/js/block-submit-on-send": {
                "formId": "contact-form"
            }
        }
    }
</script>
