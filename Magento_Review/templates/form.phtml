<?php
/**
 * Copyright © Magento, Inc. All rights reserved.
 * See COPYING.txt for license details.
 */

/** @var \Magento\Review\Block\Form $block */
if (!$block->getButtonLockManager()) {
    $objectManager = \Magento\Framework\App\ObjectManager::getInstance();
    $block->setButtonLockManager(
        $objectManager->get(\Magento\Framework\View\Element\ButtonLockManager::class)
    );
}
/** @var $escaper \Magento\Framework\Escaper */
// phpcs:disable Generic.Files.LineLength
?>
<div class="review-add mb-6">
    <div class="mb-3 border-b border-neutral-200 pb-3 text-lg font-semibold text-neutral-900"><strong><?= $escaper->escapeHtml(__('Write Your Own Review')) ?></strong></div>
<div class="text-sm text-neutral-700">
<?php if ($block->getAllowWriteReviewFlag()):?>
<form action="<?= $escaper->escapeUrl($block->getAction()) ?>" class="review-form" method="post" id="review-form" data-role="product-review-form" data-bind="scope: 'review-form'">
    <?= $block->getBlockHtml('formkey') ?>
    <?= $block->getChildHtml('form_fields_before') ?>
    <fieldset class="review-fieldset m-0 mb-6 space-y-4 rounded-lg border border-neutral-200 bg-white p-4 shadow-sm sm:p-6" data-hasrequired="<?= $escaper->escapeHtmlAttr(__('* Required Fields')) ?>">
        <legend class="review-legend mb-0 text-base font-semibold text-neutral-900"><span><?= $escaper->escapeHtml(__("You're reviewing:")) ?> </span><strong><?= $escaper->escapeHtml($block->getProductInfo()->getName()) ?></strong></legend>
        <?php if ($block->getRatings() && $block->getRatings()->getSize()): ?>
        <span id="input-message-box"></span>
        <fieldset class="review-field-ratings required mb-0 border-0 p-0">
            <legend class="mb-1 block text-sm font-medium text-neutral-800"><span><?= $escaper->escapeHtml(__('Your Rating')) ?></span></legend>
            <div class="mt-1">
                <div class="border-l border-neutral-200 pl-4" id="product-review-table">
                    <?php foreach ($block->getRatings() as $_rating): ?>
                        <div class="review-field-rating mb-4 flex items-start gap-2">
                            <label class="block text-sm font-medium text-neutral-800 mb-1" id="<?= $escaper->escapeHtml($_rating->getRatingCode()) ?>_rating_label"><span><?= $escaper->escapeHtml($_rating->getRatingCode()) ?></span></label>
                            <div class="mt-1 review-control-vote">
                            <?php $options = $_rating->getOptions();?>
                            <?php $iterator = 1; foreach ($options as $_option): ?>
                                <input
                                    type="radio"
                                    name="ratings[<?= $escaper->escapeHtmlAttr($_rating->getId()) ?>]"
                                    id="<?= $escaper->escapeHtmlAttr($_rating->getRatingCode()) ?>_<?= $escaper->escapeHtmlAttr($_option->getValue()) ?>"
                                    value="<?= $escaper->escapeHtmlAttr($_option->getId()) ?>"
                                    class="h-4 w-4 rounded border-neutral-300 text-blue-600"
                                    data-validate="{'rating-required':true}"
                                    aria-labelledby="<?= $escaper->escapeHtmlAttr($_rating->getRatingCode()) ?>_rating_label <?= $escaper->escapeHtmlAttr($_rating->getRatingCode()) ?>_<?= $escaper->escapeHtmlAttr($_option->getValue()) ?>_label" />
                                <label
                                    class="rating-<?= $escaper->escapeHtmlAttr($iterator) ?>"
                                    for="<?= $escaper->escapeHtmlAttr($_rating->getRatingCode()) ?>_<?= $escaper->escapeHtmlAttr($_option->getValue()) ?>"
                                    title="<?= $escaper->escapeHtmlAttr(__('%1 %2', $iterator, $iterator > 1 ? __('stars') : __('star'))) ?>"
                                    id="<?= $escaper->escapeHtmlAttr($_rating->getRatingCode()) ?>_<?= $escaper->escapeHtmlAttr($_option->getValue()) ?>_label">
                                    <span><?= $escaper->escapeHtml(__('%1 %2', $iterator, $iterator > 1 ? __('stars') : __('star'))) ?></span>
                                </label>
                                <?php $iterator++; ?>
                            <?php endforeach; ?>
                            </div>
                        </div>
                    <?php endforeach; ?>
                </div>
                <input type="hidden" name="validate_rating" class="validate-rating" value="" />
            </div>
        </fieldset>
    <?php endif ?>
        <div class="mb-4 review-field-nickname required">
            <label for="nickname_field" class="block text-sm font-medium text-neutral-800 mb-1"><span><?= $escaper->escapeHtml(__('Nickname')) ?></span></label>
            <div class="mt-1">
                <input type="text" name="nickname" id="nickname_field" class="w-full rounded-md border border-neutral-300 px-3 py-2 text-sm shadow-sm focus:border-blue-500 focus:ring-1 focus:ring-blue-500" data-validate="{required:true}" data-bind="value: nickname()" />
            </div>
        </div>
        <div class="mb-4 review-field-summary required">
            <label for="summary_field" class="block text-sm font-medium text-neutral-800 mb-1"><span><?= $escaper->escapeHtml(__('Summary')) ?></span></label>
            <div class="mt-1">
                <input type="text" name="title" id="summary_field" class="w-full rounded-md border border-neutral-300 px-3 py-2 text-sm shadow-sm focus:border-blue-500 focus:ring-1 focus:ring-blue-500" data-validate="{required:true}" data-bind="value: review().title" />
            </div>
        </div>
        <div class="mb-4 review-field-text required">
            <label for="review_field" class="block text-sm font-medium text-neutral-800 mb-1"><span><?= $escaper->escapeHtml(__('Review')) ?></span></label>
            <div class="mt-1">
                <textarea name="detail" id="review_field" cols="5" rows="3" class="w-full rounded-md border border-neutral-300 px-3 py-2 text-sm shadow-sm focus:border-blue-500 focus:ring-1 focus:ring-blue-500" data-validate="{required:true}" data-bind="value: review().detail"></textarea>
            </div>
        </div>
    </fieldset>
    <fieldset class="additional_info m-0 border-0 p-0">
        <?= $block->getChildHtml('form_additional_review_info') ?>
    </fieldset>
    <div class="review-form-actions clear-both flex flex-wrap gap-3 pt-2">
        <button type="submit"
                class="submit inline-flex items-center justify-center gap-2 rounded-md border border-blue-600 bg-blue-600 px-4 py-2 text-sm font-semibold text-white transition-colors hover:bg-blue-700 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-1 focus-visible:outline-blue-600 disabled:pointer-events-none disabled:opacity-50"
                <?php if ($block->getButtonLockManager()->isDisabled('review_form_submit')): ?>
                    disabled="disabled"
                <?php endif; ?>>
            <span><?= $escaper->escapeHtml(__('Submit Review')) ?></span>
        </button>
    </div>
</form>
<script type="text/x-magento-init">
{
    "[data-role=product-review-form]": {
        "Magento_Ui/js/core/app": <?= /* @noEscape */ $block->getJsLayout() ?>
    },
    "#review-form": {
        "Magento_Review/js/error-placement": {},
        "Magento_Review/js/validate-review": {},
        "Magento_Review/js/submit-review": {}
    }
}
</script>
<?php else: ?>
    <div class="notlogged mb-4 rounded-md border border-neutral-200 bg-neutral-50 px-4 py-3 text-sm text-neutral-800" id="review-form">
        <div>
            <?= $escaper->escapeHtml(__('Only registered users can write reviews. Please <a href="%1">Sign in</a> or <a href="%2">create an account</a>', $block->getLoginLink(), $block->getRegisterUrl()), ['a']) ?>
        </div>
    </div>
<?php endif ?>
</div>
</div>
