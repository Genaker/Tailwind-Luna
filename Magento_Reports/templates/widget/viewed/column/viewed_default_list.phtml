<?php
declare(strict_types=1);
/**
 * Copyright © Magento, Inc. All rights reserved.
 * See COPYING.txt for license details.
 */

/**
 * @deprecated
 * @var \Magento\Reports\Block\Product\Viewed $block
 */
?>
<?php
if ($exist = ($block->getRecentlyViewedProducts() && $block->getRecentlyViewedProducts()->getSize())) {
    $type = 'widget-viewed';
    $mode = 'list';

    $type = $type . '-' . $mode;

    $image = 'cart_page_product_thumbnail';
    $title = __('Recently Viewed');
    $items = $block->getRecentlyViewedProducts();

    $showWishlist = false;
    $showCompare = false;
    $showCart = false;
    $rating = false;
    $description = false;
}
?>
<?php if ($exist) : ?>
<div class="block widget block-viewed-products-<?= $block->escapeHtmlAttr($mode) ?>">
    <div class="text-lg font-semibold text-neutral-900 mb-3 pb-2 border-b border-neutral-200">
        <strong><?= $block->escapeHtml($title) ?></strong>
    </div>
    <div class="text-sm text-neutral-700">
        <?php $suffix = $block->getNameInLayout(); ?>
        <ol class="grid grid-cols-1 gap-6 sm:grid-cols-2 xl:grid-cols-3 list-none p-0 m-0" id="widget-viewed-<?= $block->escapeHtmlAttr($suffix) ?>">
            <?php foreach ($items as $_product) : ?>
                <li class="flex flex-col border border-neutral-200 rounded-lg bg-white overflow-hidden">
                    <div class="flex flex-col gap-3 p-4">
                        <a class="block overflow-hidden bg-neutral-100" href="<?= $block->escapeUrl($_product->getProductUrl()) ?>"
                           title="<?= $block->escapeHtmlAttr($block->stripTags($_product->getName(), null, true)) ?>">
                            <?= $block->getImage($_product, $image)->toHtml() ?>
                        </a>
                        <div class="flex flex-col gap-2 px-4 pb-4">
                            <strong class="text-base font-semibold leading-snug">
                                <a href="<?= $block->escapeUrl($_product->getProductUrl()) ?>"
                                   title="<?= $block->escapeHtmlAttr($block->stripTags($_product->getName(), null, true)) ?>"
                                   class="text-blue-600 hover:text-blue-800 hover:underline">
                                    <?= $block->escapeHtml($outputHelper->productAttribute($_product, $_product->getName(), 'name')) ?>
                                </a>
                            </strong>
                            <?= /* @noEscape */ $block->getProductPriceHtml(
                                $_product,
                                \Magento\Catalog\Pricing\Price\FinalPrice::PRICE_CODE,
                                \Magento\Framework\Pricing\Render::ZONE_ITEM_LIST,
                                [
                                    'price_id_suffix' => '-widget-viewed-' . $suffix
                                ]
                            ) ?>
                            <div class="flex flex-wrap items-center gap-2">
                                <?php if ($_product->isSaleable()) : ?>
                                <div class="flex flex-wrap gap-2">
                                    <?php if ($_product->getTypeInstance()->hasRequiredOptions($_product)) : ?>
                                        <button class="inline-flex items-center justify-center gap-2 rounded-md text-sm font-semibold border px-4 py-2 transition-colors focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-1 focus-visible:outline-blue-600 disabled:opacity-50 disabled:pointer-events-none bg-blue-600 text-white border-blue-600 hover:bg-blue-700 tocart"
                                                data-mage-init='{"redirectUrl": {"url": "<?= $block->escapeJs($block->escapeUrl($block->getAddToCartUrl($_product))) ?>"}}'
                                                type="button" title="<?= $block->escapeHtmlAttr(__('Add to Cart')) ?>">
                                            <span><?= $block->escapeHtml(__('Add to Cart')) ?></span>
                                        </button>
                                    <?php else : ?>
                                        <?php
                                            $postData = $postDataHelper->getPostData($block->getAddToCartUrl($_product), ['product' => $_product->getEntityId()]);
                                        ?>
                                        <button type="button" class="inline-flex items-center justify-center gap-2 rounded-md text-sm font-semibold border px-4 py-2 transition-colors focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-1 focus-visible:outline-blue-600 disabled:opacity-50 disabled:pointer-events-none bg-blue-600 text-white border-blue-600 hover:bg-blue-700 tocart" data-post='<?= /* @noEscape */ $postData ?>'>
                                            <span><?= $block->escapeHtml(__('Add to Cart')) ?></span>
                                        </button>
                                    <?php endif; ?>
                                </div>
                                <?php else : ?>
                                    <?php if ($_product->isAvailable()) : ?>
                                        <div class="text-sm text-green-700"><span><?= $block->escapeHtml(__('In stock')) ?></span></div>
                                    <?php else : ?>
                                        <div class="text-sm text-red-700"><span><?= $block->escapeHtml(__('Out of stock')) ?></span></div>
                                    <?php endif; ?>
                                <?php endif; ?>
                            </div>
                        </div>
                    </div>
                </li>
            <?php endforeach; ?>
        </ol>
    </div>
</div>
<?php endif; ?>
