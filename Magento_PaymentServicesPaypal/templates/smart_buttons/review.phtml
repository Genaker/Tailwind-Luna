<?php
/**
 * Copyright © Magento, Inc. All rights reserved.
 * See COPYING.txt for license details.
 */
declare(strict_types=1);

?>

<?php
// phpcs:disable Magento2.Files.LineLength, Generic.Files.LineLength
/** @var \Magento\PaymentServicesPaypal\Block\SmartButtons\Review $block */
/** @var \Magento\Framework\Escaper $escaper */
/** @var \Magento\Framework\View\Helper\SecureHtmlRenderer $secureRenderer */
?>

<div class="magento-payments-review view">
    <div class="mb-8">
        <div class="text-sm text-neutral-700">
            <?php if ($block->getShippingAddress()): ?>
                <div class="mb-6 rounded-lg border border-neutral-200 bg-white box-order-shipping-method">
                    <strong class="px-4 py-3 text-base font-semibold border-b border-neutral-200 bg-neutral-50">
                        <span><?= $escaper->escapeHtml(__('Shipping Method')) ?></span>
                    </strong>
                    <div class="p-4">
                        <form method="post"
                              id="shipping-method-form"
                              action="<?= $escaper->escapeUrl($block->getShippingMethodSubmitUrl()) ?>"
                              class="form"
                        >
                            <?php if ($block->canEditShippingMethod()): ?>
                                <?php if ($groups = $block->getShippingRateGroups()): ?>
                                    <?php $currentRate = $block->getCurrentShippingRate() ?>
                                    <div class="mb-4 shipping required">
                                        <select name="shipping_method" id="shipping-method" class="w-full rounded-md border border-neutral-300 px-3 py-2 text-sm bg-white">
                                            <?php if (!$currentRate): ?>
                                                <option value="">
                                                    <?= $escaper->escapeHtml(__('Please select a shipping method...')) ?>
                                                </option>
                                            <?php endif ?>
                                            <?php foreach ($groups as $code => $rates): ?>
                                                <optgroup label="<?= $escaper->escapeHtml($block->getCarrierName($code)) ?>">
                                                    <?php foreach ($rates as $rate): ?>
                                                        <option
                                                            value="<?= $escaper->escapeHtmlAttr($block->renderShippingRateValue($rate)) ?>"
                                                            <?php if ($currentRate === $rate): ?>selected="selected"<?php endif ?>
                                                        >
                                                            <?= /* @noEscape */ $block->renderShippingRateOption($rate) ?>
                                                        </option>
                                                    <?php endforeach ?>
                                                </optgroup>
                                            <?php endforeach ?>
                                        </select>
                                    </div>
                                <?php else: ?>
                                    <p>
                                        <?= $escaper->escapeHtml(
                                            __('Sorry, no quotes are available for this order right now.')
                                        ) ?>
                                    </p>
                                <?php endif ?>
                            <?php else: ?>
                                <p>
                                    <?= /* @noEscape */ $block->renderShippingRateOption(
                                        $block->getCurrentShippingRate()
                                    ) ?>
                                </p>
                            <?php endif ?>
                        </form>
                    </div>
                </div>
                <div class="mb-6 rounded-lg border border-neutral-200 bg-white box-order-shipping-address">
                    <strong class="px-4 py-3 text-base font-semibold border-b border-neutral-200 bg-neutral-50">
                        <span><?= $escaper->escapeHtml(__('Shipping Address')) ?></span>
                    </strong>
                    <div class="p-4">
                        <address>
                            <?= $escaper->escapeHtml(
                                $block->renderAddress($block->getShippingAddress()),
                                ['br']
                            );?>
                        </address>
                    </div>
                    <?php if ($block->getCanEditShippingAddress()): ?>
                        <div class="px-4 py-3 border-t border-neutral-200 flex flex-wrap gap-2 justify-end">
                            <a href="<?= $escaper->escapeUrl($block->getEditUrl()) ?>" class="inline-flex items-center justify-center gap-2 rounded-md text-sm font-semibold border px-4 py-2 transition-colors focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-1 focus-visible:outline-blue-600 disabled:opacity-50 disabled:pointer-events-none edit">
                                <span><?= $escaper->escapeHtml(__('Edit')) ?></span>
                            </a>
                        </div>
                    <?php endif ?>
                </div>
            <?php endif ?>
            <div class="mb-6 rounded-lg border border-neutral-200 bg-white box-order-billing-address">
                <strong class="px-4 py-3 text-base font-semibold border-b border-neutral-200 bg-neutral-50"><span><?= $escaper->escapeHtml(__('Payment Method')) ?></span></strong>
                <div class="p-4">
                    <?= $escaper->escapeHtml($block->getPaymentMethodTitle()) ?><br>
                    <?= $escaper->escapeHtml($block->getEmail()) ?> <br>
                    <img class="payment-source-logo max-h-8 w-auto" src="<?= $escaper->escapeUrl($block->getPaymentIcon()) ?>" alt="<?= $escaper->escapeHtmlAttr($block->getPaymentSource()) ?>"/>
                </div>
            <?php if ($block->getEditUrl()): ?>
                <div class="px-4 py-3 border-t border-neutral-200 flex flex-wrap gap-2 justify-end">
                    <a href="<?= $escaper->escapeUrl($block->getEditUrl()) ?>" class="inline-flex items-center justify-center gap-2 rounded-md text-sm font-semibold border px-4 py-2 transition-colors focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-1 focus-visible:outline-blue-600 disabled:opacity-50 disabled:pointer-events-none edit">
                        <span><?= $escaper->escapeHtml(__('Edit Payment Information')) ?></span>
                    </a>
                </div>
            <?php endif ?>
            </div>
        </div>
    </div>

    <div class="magento-payments-review-discount">
        <?= $block->getChildHtml('paymentservicespaypal.additional.actions') ?>
    </div>

    <div class="magento-payments-review-items">
        <div class="magento-payments-review-title flex flex-wrap items-center justify-between gap-3 border-b border-neutral-200 pb-3 mb-4">
            <strong class="text-base font-semibold text-neutral-900"><?= $escaper->escapeHtml(__('Items in Your Shopping Cart')) ?></strong>
            <a href="<?= $escaper->escapeUrl($block->getUrl('paymentservicespaypal/smartbuttons/cancel')) ?>" class="inline-flex items-center justify-center gap-2 rounded-md text-sm font-semibold border px-4 py-2 transition-colors focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-1 focus-visible:outline-blue-600 disabled:opacity-50 disabled:pointer-events-none edit">
                <span><?= $escaper->escapeHtml(__('Edit Shopping Cart')) ?></span>
            </a>
        </div>

        <?= $block->getChildHtml('details') ?>

        <form method="post" id="order-review-form" action="<?= $escaper->escapeUrl($block->getPlaceOrderUrl()) ?>" class="form order-review-form">
            <?= $block->getChildHtml('agreements') ?>
                <div class="flex flex-wrap items-center justify-between gap-4 py-4 clear-both" id="review-buttons-container">
                <div>
                    <button type="button" id="review-button" class="inline-flex items-center justify-center gap-2 rounded-md text-sm font-semibold border px-4 py-2 transition-colors focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-1 focus-visible:outline-blue-600 disabled:opacity-50 disabled:pointer-events-none bg-blue-600 text-white border-blue-600 hover:bg-blue-700 checkout"
                            value="<?= $escaper->escapeHtmlAttr(__('Place Order')) ?>">
                        <span><?= $escaper->escapeHtml(__('Place Order')) ?></span>
                    </button>
                </div>
                <span class="please-wait load indicator" id="review-please-wait"
                      data-text="<?= $escaper->escapeHtmlAttr(__('Submitting order information...')) ?>">
                   <span><?= $escaper->escapeHtml(__('Submitting order information...')) ?></span>
                </span>
                <?= /* @noEscape */ $secureRenderer->renderStyleAsTag("display: none;", 'span#review-please-wait')?>
            </div>
        </form>
    </div>
</div>
<script type="text/x-magento-init">
    {
        "#order-review-form": {
            "validation": {},
            "Magento_PaymentServicesPaypal/js/order-review": {
                "updateShippingMethodUrl": "<?= $escaper->escapeJs($block->getShippingMethodSubmitUrl()) ?>",
                "canEditShippingMethod": <?= $block->canEditShippingMethod() ? 'true' : 'false' ?>,
                "isVirtual": <?= /* @noEscape */ $block->getIsQuoteVirtual() ?>,
                "editShoppingCartSelector": ".magento-payments-review-items .edit"
            }
        }
    }
</script>
